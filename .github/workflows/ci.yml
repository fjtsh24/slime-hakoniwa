name: CI

on:
  pull_request:
    branches: [main, develop]
  push:
    branches: [develop]

jobs:
  test-frontend:
    name: Frontend Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json
      - name: Install dependencies
        run: cd frontend && npm ci
      - name: Lint
        run: cd frontend && npm run lint
      - name: Build
        run: cd frontend && npm run build
      - name: Test with coverage
        run: cd frontend && npm run test:coverage
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: frontend-test-report
          path: tests/reports/frontend-coverage/

  test-functions:
    name: Functions Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: functions/package-lock.json
      - name: Install dependencies
        run: cd functions && npm ci
      - name: Lint
        run: cd functions && npm run lint
      - name: Build
        run: cd functions && npm run build
      - name: Test with coverage (unit only)
        run: cd functions && npx jest --testPathPattern="tests/unit" --coverage --forceExit --coverageReporters=html --coverageDirectory=../tests/reports/functions-coverage
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: functions-test-report
          path: tests/reports/functions-coverage/
