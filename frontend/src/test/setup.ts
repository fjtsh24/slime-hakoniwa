// NOTE: @testing-library/jest-dom は vitest の globals: true でカバーされるが、
// マッチャー型定義（expect(el).toBeInTheDocument() など）を使用するには
// `npm install -D @testing-library/jest-dom` でインストールした上で、
// このファイルに `import '@testing-library/jest-dom'` を追加してください。
